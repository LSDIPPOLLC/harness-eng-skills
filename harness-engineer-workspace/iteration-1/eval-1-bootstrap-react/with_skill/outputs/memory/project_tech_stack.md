---
name: tech-stack
description: React + TypeScript + Vite frontend with Jest tests, ESLint + Prettier, deployed to Vercel
type: project
---

Core stack: React, TypeScript (strict), Vite bundler.

Testing: Jest + React Testing Library.

Code quality: ESLint for linting, Prettier for formatting.

Deployment: Vercel with automatic preview deploys on PRs.

Structure: ~200 files, standard src/components layout.

**Why:** Understanding the stack avoids suggesting incompatible tools or patterns (e.g., don't suggest webpack config, don't suggest Vitest if Jest is configured).
